const GlassMorphimsButton = ({ color }) => {
  return (
    <div className="container">
      <div className="btn" id={color}>
        <a href="#">Read More</a>
      </div>
    </div>
  );
};

export default GlassMorphimsButton;
